<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera Feature & Permission (ONCE ONLY) -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Internet & Network (ONCE ONLY) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Biometric -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Storage for profile pictures -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Push Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PantryPaloneVo"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication Activities -->
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SimpleLoginActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ForgotPasswordActivity" />

        <!-- Main App Activities -->
        <activity android:name=".HomePageActivity" />
        <activity android:name=".FingerprintActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".AccountSettingsActivity" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".OrderHistoryActivity" />
        <activity android:name=".DonorDashboardActivity" />

        <!-- ✅ NEW: Recipe Activity -->
        <activity
            android:name=".RecipeActivity"
            android:exported="false" />
        <activity
            android:name=".RecipeDetailActivity"
            android:exported="false"
            android:parentActivityName=".RecipeActivity" />
        
        <!-- Shared Recipe View Activity (Public Access) -->
        <activity
            android:name=".SharedRecipeViewActivity"
            android:exported="true"
            android:parentActivityName=".RecipeActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Deep link: pantrypal://recipe/{recipeId} -->
                <data
                    android:scheme="pantrypal"
                    android:host="recipe" />
                <!-- Web link: https://pantrypal.app/recipe/{recipeId} -->
                <data
                    android:scheme="https"
                    android:host="pantrypal.app"
                    android:pathPrefix="/recipe" />
            </intent-filter>
        </activity>

        <!-- Pantry Related Activities -->
        <activity android:name=".PublishPantryActivity" />
        <activity
            android:name=".ItemDetailsActivity"
            android:exported="false"
            android:parentActivityName=".PublishPantryActivity" />
        <activity
            android:name=".ReviewSavedItemsActivity"
            android:exported="false"
            android:parentActivityName=".PublishPantryActivity" />
        <activity android:name=".PublishPantryFormActivity" />
        <activity android:name=".PantryLocationActivity" />
        <activity android:name=".PantryProductsActivity" />
        <activity android:name=".ReviewProductsActivity" />
        <activity android:name=".ConfirmProductsActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Google Places API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_places_api_key" />

        <!-- Detection Activities -->
        <activity android:name=".RealtimeDetectionActivity" />

        <!-- Pantry Detector -->
        <activity
            android:name=".PantryDetectorActivity"
            android:screenOrientation="portrait"
            android:exported="false" />

        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Default notification icon -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@android:drawable/ic_dialog_info" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@android:color/transparent" />

    </application>

</manifest>
